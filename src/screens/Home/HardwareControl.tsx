import React from "react";
import { Text, View } from "react-native";

import { ScreenWrapper, Typo } from "@/components";

const HardwareControl = () => {
  return (
    <ScreenWrapper>
      <Typo className="text-3xl text-center">Hardware Control</Typo>
    </ScreenWrapper>
  );
};

export default HardwareControl;
